buildscript {
    repositories {
        jcenter()
        maven { url "https://plugins.gradle.org/m2/" }
    }
    dependencies {
        classpath("org.owasp:dependency-check-gradle:5.2.4")
    }
}
apply plugin: 'org.owasp.dependencycheck'

dependencyCheck {
    skipConfigurations = ["gretty"]
}
